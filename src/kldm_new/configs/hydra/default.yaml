run:
  dir: ${paths.log_dir}/${project}/runs/${now:%Y-%m-%d}_${now:%H-%M-%S}

sweep:
  dir: ${paths.log_dir}/${project}/multiruns/${now:%Y-%m-%d}_${now:%H-%M-%S}
  subdir: ${hydra.job.num}

job:
  chdir: true
